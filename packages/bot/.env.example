# ==================================================
# VALKI TALKI — Environment Configuration
# Production (Railway)
# ==================================================

# --------------------------------------------------
# SERVER
# --------------------------------------------------
NODE_ENV=production
PORT=3000

# --------------------------------------------------
# CORS / FRONTEND ORIGINS
# --------------------------------------------------
# Comma-separated list of allowed frontend origins
CORS_ORIGINS=https://valki.wiki,https://www.valki.wiki,https://auth.valki.wiki

# --------------------------------------------------
# INPUT / OUTPUT LIMITS
# --------------------------------------------------
MAX_INPUT_CHARS=800
MAX_OUTPUT_TOKENS=800

# --------------------------------------------------
# DATABASE (Postgres)
# --------------------------------------------------
# IMPORTANT:
# In Railway → Variables:
#   DATABASE_URL = ${{ Postgres.DATABASE_URL }}
#
# Leave EMPTY here in production
DATABASE_URL=

# --------------------------------------------------
# VALKI CORE / OPENAI
# --------------------------------------------------
OPENAI_API_KEY=
VALKI_PROMPT_ID=pmpt_693178eb05908195a3590644b1ec8d830dc3ffd3f6a56de9

# --------------------------------------------------
# AUTH / SESSION TOKENS
# --------------------------------------------------
# Used to sign Valki auth/session tokens
# Generate once, keep secret forever
#
# Example:
#   openssl rand -hex 32
AUTH_TOKEN_SECRET=

# --------------------------------------------------
# DISCORD OAUTH (LOGIN)
# --------------------------------------------------
# Discord Developer Portal → OAuth2
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# MUST EXACTLY MATCH Discord OAuth redirect URI
# Example:
#   https://valki-bot-production.up.railway.app/auth/discord/callback
DISCORD_REDIRECT_URI=

# --------------------------------------------------
# GOOGLE OAUTH (LOGIN)
# --------------------------------------------------
# Google Cloud Console → OAuth 2.0 Client
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# MUST EXACTLY MATCH Google OAuth redirect URI
# Example:
#   https://valki-bot-production.up.railway.app/auth/google/callback
GOOGLE_REDIRECT_URI=

# --------------------------------------------------
# DISCORD BOT (OPTIONAL)
# --------------------------------------------------
# Only needed if Valki replies inside Discord servers
DISCORD_TOKEN=

# Optional: restrict to specific Discord channel IDs
# Comma-separated, leave empty = all channels
CHANNEL_IDS=
